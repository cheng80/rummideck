# 개발 플랜 체크리스트

> **이 파일이 실행 단위 진행표다.** 완료 시 `- [ ]`를 `- [x]`로 바꾼다.  
> 재점검 기준일: **2026-04-06** (코드·문서 기준 스냅샷)  
> **계획 수정 (2026)**: 순수 로직만으로는 재미(게임성) 검증이 어렵다고 판단, **한 번의 완전한 순회**(첫 전투~1차 보스)와 **필수 피드백·연출**을 검증 목표에 포함한다. (아래 §0 참고)
> **전략 수정 (2026-04-05)**: 현재 게임 로직은 부분 수정이 아니라, **문서 기준으로 처음부터 다시 짠다**는 전제로 재정렬한다. 기존 구현은 참고용이며, 아래 §0.5 재작성 플랜을 현재 우선 기준으로 본다.

---

## 진행 요약 (한눈에)

| 영역 | 상태 |
|------|------|
| Pure Dart 코어 (타일·족보·점수·RNG) | 대부분 완료 |
| 런 루프 (RunContext·스테이지·손패·플레이/버리기) | 완료 |
| 상점·MVP 변칙(8종) | 로직 위주 완료 |
| Flutter/Flame 게임 화면·HUD·오버레이 | 기본 완료 |
| **완전 순회 게임성 (딜·드로우·리필·점수 연출 + 첫 전투~1차 보스)** | **미완 — 우선 과제** |
| 시드 UI·표시 | **미완** (코드상 고정 문자열) |
| 문서 기준 진행 (Ante/Blind 3단 등) | **미완** |
| Balatro급 족보/문서·코드 완전 정합 | 검토·추가 작업 필요 |

상세 근거: `docs/current-implementation-status.md`

---

## 0. 게임성 검증 전략 (계획 수정)

### 이전

- 로직·수치만 맞으면 UX·애니메이션은 나중에 두어도 된다고 가정.

### 현재

- **재미는 연출·리듬·피드백이 없으면 검증할 수 없다.**
- **최소 목표**: **조커(Jester)를 쓰지 않는 첫 전투부터 1차 보스전까지** **한 번의 완전한 순회**가 **끊김 없이** 플레이되고, 아래 **움직임·피드백**이 들어간 상태에서만 “게임성 검증”으로 친다.

### 시각·레이아웃 참고 (원작 Balatro 느낌)

- UI·카드 배치·전투 화면 구성 참고용: [MAIZ — Balatro(발라트로) 리뷰 (네이버 블로그)](https://m.blog.naver.com/madmaiz/223680967644)  
  (해당 글의 **스크린샷**을 레이아웃·밀도·정보 배치 참고로 활용. 저작권이 있는 원본 일러스트를 무단 복제하지 말고, **구성·비율·UX 목표**로만 사용.)

### 완전 순회 완료 정의 (체크 기준)

- [ ] **첫 전투 ~ 1차 보스** 구간을 **Jester 의존 없이** 클리어 가능한 난이도·룰로 통과 가능
- [ ] 아래 **§5.1 연출** 항목이 체크된 뒤, 이 절의 “**한 번의 완전한 순회** 완료”를 **함께** 판정

### 0.5 로직 재작성 플랜 (현재 최우선)

> 목표: 현재 구현을 부분 보수하지 않고, **문서 기준 단일 규칙 흐름**으로 Pure Dart 게임 로직을 다시 세운다.

- [x] **재작성 기준선 문서 확정**: 현재 우선 문서(`10`, `20`, `21`, `30`, `40`, `50`)에서 실제 구현 대상으로 쓸 규칙만 다시 잠근다 (`docs/logic-rewrite-baseline.md`)
- [ ] **도메인 모델 재설계**: Tile / Hand / Blind / Ante / Shop / Jester / RunState 경계를 다시 정의한다
- [ ] **핸드 판정기 재작성**: 문서 기준 족보와 포함 관계를 다시 구현하고 예외 규칙(Ace, Pair, 확장 족보)을 분리한다
- [ ] **점수 파이프라인 재작성**: Base Chips / Number Bonus / Mult / XMult / Jester 트리거 순서를 이벤트 기반으로 다시 세운다
  선택 타일 중 실제 조합 타일만 점수 계산하고, 남는 타일은 discard 처리하는 규칙 포함
- [ ] **진행 구조 재작성**: Ante × Blind(Small/Big/Boss), 스킵, 보상, Boss 제약을 `RunContext`와 분리 가능한 구조로 다시 설계한다
- [ ] **상점/Jester 런타임 재작성**: `jesters_common.json` 기반 카탈로그 로더, 구매/리롤/슬롯/희귀도 규칙을 다시 연결한다
- [ ] **번역 데이터 로딩 연결**: `assets/translations/data/en|ko/jesters.json`를 `displayName/effectText/notes` fallback 구조로 읽는다
- [ ] **기존 구현 폐기 범위 확정**: 재사용할 파일과 버릴 파일을 목록화하고, 임시 호환 계층 필요 여부를 결정한다
- [ ] **재작성 테스트 기준선 구축**: evaluator / score / progression / shop / localization 단위 테스트를 새 기준으로 다시 세운다
- [ ] **UI 재연결 최소 경로 확보**: 새 로직으로 게임 화면이 다시 뜨는 최소 세션(start stage, select, submit, shop open)을 우선 복구한다

### 0.6 문서 대비 현재 불일치 재점검

> 아래 항목은 “지금 돌아간다”와 별개로, **문서 기준 재작성 전 반드시 바로잡아야 할 불일치**다.

- [ ] **Hands per Blind 불일치 정리**: 현재 기본 `playsLeft = 5`를 문서 기준 `4`로 재정의하거나, 반대로 문서를 수정할지 결정한다
- [ ] **Jester 슬롯 수 불일치 정리**: 현재 런타임 구매 슬롯 `3`을 문서 기준 `5`로 올릴 구조를 확정한다
- [ ] **Anomaly 용어/타입 제거 계획 확정**: `Anomaly` 명칭과 클래스 의존을 `Jester` 기준으로 치환할 파일 목록을 잠근다
- [ ] **Ante × Blind 진행 구조 불일치 정리**: 현재 stage 1~5 직진 구조를 `Small / Big / Boss` 3단으로 재설계한다
- [ ] **점수 파이프라인 용어 정리**: 현재 `anomaly` 기반 score 단계명을 `Jester Chips / Mult / XMult` 기준으로 문서·코드에서 일치시킨다
- [ ] **Run Info 조합표 규칙 확정**: Planet 기반 레벨업 규칙과 표시 필드(`Lv`, `칩`, `멀트`, `업그레이드`)를 문서 기준으로 잠근다

---

## 1. 프로젝트 기반

- [x] Flutter + Flame 프로젝트 구성
- [x] 라우팅 (타이틀 / 게임 / 설정)
- [x] BGM·SFX·화면 꺼짐 방지 등 기본 설정
- [ ] **시드 입력 UI** (런 시작 전 문자열 입력·검증)
- [ ] **시드 표시** (HUD 또는 설정에서 현재 시드·복사)
- [ ] **고정 시드 제거**: `GameView`의 임시 `'MVP-001'` 고정값을 없애고, 랜덤 기본 시드 또는 사용자 입력 시드로 런 시작

---

## 2. 코어 로직 (Pure Dart)

- [x] Tile / 색상 / `StandardDeck` (52타일)
- [x] `CombinationEvaluator` + `CombinationType` (족보 판정)
- [x] `ScoreCalculator` + Chips / Mult / XMult 파이프라인
- [x] `SeededRng` + 시드 문자열 기반 재현
- [x] `StageTargetCalculator` (스테이지 목표 점수)
- [ ] **`10_game_rules.md`와 족보 정의 일치 검토** (5카드 포커 핸드 vs 현재 선택 규칙·이름 매핑)
- [x] **Two Pair 등 문서·테스트·점수표** 누락분 보강 (`20_score_system.md` 기준)
- [ ] **비조합 Play 허용 규칙 반영**: 유효 조합이 없어도 `High Tile`로 제출 가능하게 처리
- [ ] **부분 조합 제출 규칙 반영**: 선택 타일 중 최고 우선순위 조합만 점수 계산하고, 나머지는 discard 처리

---

## 3. 런 상태·게임 루프

- [x] `RunContext` / `PlayerState` / `StageState`
- [x] 손패 8장·보충·덱/버림패
- [x] 조합 제출·점수 누적·스테이지 클리어/실패
- [x] Hands / Discards 소모 (런 규칙에 맞게)
- [x] 골드 지급·상점 페이즈 진입 (`RunPhase.shop` 등)
- [ ] **첫 전투~1차 보스**까지 내러티브·난이도·목표 점수가 **완전 순회 검증**에 맞게 조정됨 (임시라도 문서 1줄)
- [ ] **`30_progression.md` 수준: Ante × Blind(Small/Big/Boss) 3라운드 구조**
- [ ] **Boss 블라인드 제약·스킵·보상 분리** (문서대로)
- [x] 같은 런에서 다음 스테이지 시작 시 손패를 버림패로 넘기고 새 손패를 뽑는다

---

## 4. 상점·변칙 타일 (Jester / Anomaly)

- [x] `ShopState` / 오퍼 / 리롤 비용 증가
- [x] 구매·판매·슬롯 교체 (MVP)
- [x] MVP용 변칙 데이터 (`mvp_anomalies` 등)
- [x] Jester 데이터 번역 파일 준비 (`assets/translations/data/en|ko/jesters.json`)
- [ ] **`data/common/jesters_common.json` ↔ 게임 로직 연동** (카탈로그·`22_jester_catalog.md` 기준)
- [ ] **임시 `Anomaly` 기반 UI/상점/런타임 제거 후 JSON 기반 Jester 시스템으로 교체**
- [ ] **Jester 번역 키 로더 연결** (`displayNameKey` / `effectTextKey` / `notesKey`)
- [ ] **Jester 슬롯 5개 런타임 반영** (기본 4 + 확장 1 표현 포함)
- [ ] 에디션·스티커·희귀도 상점 확률 (`21`·`40`) 정밀 구현  
- [ ] **완전 순회 검증 이후** Jester 빌드 다양성·상점 UX를 본격 튜닝 (§0 완료 뒤 우선순위 상향)

---

## 5. UI · Flame

- [x] `GameView` + `GameSessionController` 연동
- [x] 상단 HUD·손패·선택·제출/버리기·정렬
- [x] 변칙 슬롯·조합/점수 미리보기
- [x] 상점 / 게임오버 / 일시정지 오버레이
- [x] Flame `SampleGame` 배경 레이어
- [ ] **`50_architecture.md` 11절: 무스크롤 한 화면·가독성 검증** (기기별)
- [ ] 실시간 HUD 성능 (Flame vs Flutter — 성능 가이드 준수)

### 5.1 게임성 검증 필수 — 카드(타일) 피드백·연출

> §0 **한 번의 완전한 순회**와 묶어서 완료 판정. **순수 숫자 갱신만**이면 미완료.

- [ ] **초기 딜 / 드로우**: 덱에서 손패로 타일이 들어올 때 **이동·등장**이 보임 (간단한 트윈·슬라이드 등)
- [ ] **플레이 후 리필**: 제출·버리기 뒤 손패가 8장으로 맞춰질 때 **추가 드로우**가 구분됨
- [ ] **점수 계산 연출**: 제출 시 Chips → Mult → XMult → 합계로 이어지는 흐름이 **순차적으로 읽힘** (숫자 카운트·팝업 등 최소 구현)
- [ ] **손패·플레이 영역 배치**: 참고 링크(§0) 수준으로 **한 화면에서 손패·목표·점수·액션**이 동시에 파악 가능
- [ ] **보스 전 직전**: 1차 보스 진입 시 목표·제약이 **전투 맥락**에서 이해되는 전환 (전환 연출 또는 전용 UI 블록)
- [x] **연출 중 입력 차단 기반**: 드로우 애니메이션 중 최상단 투명 패널로 입력을 잠글 수 있는 공통 레이어 기반 추가

---

## 6. 테스트 · 품질

- [x] `combination_evaluator_test` / `score_calculator_test` / `seeded_rng_test`
- [x] `run_context_test` / `shop_state_test` / `stage_target_calculator_test`
- [x] `game_session_controller_test`
- [ ] 위젯/골든 테스트 (게임 화면 핵심 경로)
- [ ] `70_playtest.md` 기준 수동 런 로그 10~20회 (**연출 포함 빌드** 기준으로 갱신)

---

## 7. 문서 · 데이터 정합

- [ ] `docs/current-implementation-status.md`와 본 체크리스트 동기화 (마일스톤마다)
- [x] Gold·Pair 정책 문서 확정 및 현재 코드 반영 상태 동기화
- [x] Jester 로컬라이징용 데이터 파일 분리 (`en` 원문 / `ko` 번역 템플릿+실데이터)
- [ ] 덱 구성(단일 52 vs 멀티) 문서 확정

---

## 8. MVP 완료 판정 (`80_mvp_checklist.md` 연동)

- [ ] **§0 완전 순회 + §5.1 연출** 충족을 MVP “게임성 검증” 전제로 반영 (`80_mvp_checklist.md` 문구와 대조·필요 시 수정)
- [ ] 위 항목 중 **MVP 범위에 해당하는 것**을 `80_mvp_checklist.md`와 대조해 전부 충족했는지 확인
- [ ] 스토어/내부 배포 전 **체크리스트 서명(날짜)**

---

## 9. MVP 이후 (백로그)

- [ ] 변칙 타일 대량 확장·밸런스
- [ ] 메타 업그레이드·챌린지·난이도(Stake) UI
- [ ] 고급 이펙트·사운드 디테일 (§5.1 최소 연출 **이후**)

---

## 사용법

1. **새 대화·작업 재개 시** 먼저 [`START_HERE.md`](START_HERE.md)를 읽고, 곧바로 본 체크리스트로 다음 할 일을 잡는다.
2. 작업을 끝낼 때마다 해당 `- [ ]`를 `- [x]`로 수정한다.
3. 새 작업이 생기면 섹션에 항목을 **추가**한다 (목표 리스트만 있고 체크가 없으면 안 됨).
4. 큰 방향은 `docs/Rumikub_Game/01_plans.md` + **본 파일**이 실제 진행의 근거다.
